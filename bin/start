#!/bin/bash
#
# Start dops with zsh, nothing to run go-apish or another command.
#
set -eu

case "$@" in
    "zsh") 
        nohup go-apish > /var/log/go-apish 2>&1 &
        exec zsh
        ;;
    "")
        exec go-apish ;;
    "$@")
        exec "$@" ;;
esac
